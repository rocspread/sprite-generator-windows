const RATIOES = [1, 2, 3, 4];

const NAME_SPACE_REG = /^[A-Za-z0-9-_]+.svg$/;

const SVG_SPRITER_CONFIG = {
    dest: 'out',
    mode: {
        css: {}
    }
};

module.exports = {
    RATIOES,
    NAME_SPACE_REG,
    SVG_SPRITER_CONFIG
};